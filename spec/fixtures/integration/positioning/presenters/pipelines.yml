presenter:
  name: pipelines
  model: pipeline
  label: Pipelines
  slug: pipelines
  index:
    table_columns:
      - field: name
        sortable: true
  show:
    layout:
      - section: Details
        fields:
          - field: name
  form:
    sections:
      - title: Details
        fields:
          - field: name
  actions:
    collection:
      - name: create
        type: built_in
    single:
      - name: show
        type: built_in
      - name: edit
        type: built_in
      - name: destroy
        type: built_in
