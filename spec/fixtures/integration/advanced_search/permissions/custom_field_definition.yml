permissions:
  model: custom_field_definition

  roles:
    admin:
      crud: [index, show, create, update, destroy]
      fields:
        readable: all
        writable: all
      actions: all
      scope: all
      presenters: all
