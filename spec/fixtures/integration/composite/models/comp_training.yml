model:
  name: comp_training
  label: "Training"

  fields:
    - name: employee_id
      type: integer
    - name: title
      type: string
    - name: training_date
      type: date
    - name: status
      type: string

  options:
    timestamps: true
